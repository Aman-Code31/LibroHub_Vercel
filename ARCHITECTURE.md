# Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    LIBRARY MANAGEMENT SYSTEM                 │
│                      (Separated Architecture)                │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────────┐         ┌──────────────────────────┐
│       FRONTEND           │         │        BACKEND           │
│    (Port 8080)           │         │     (Port 3000)          │
├──────────────────────────┤         ├──────────────────────────┤
│                          │         │                          │
│  📄 HTML Files           │         │  🚀 Express Server       │
│    ├─ index.html         │         │                          │
│    ├─ dashboard.html     │         │  📡 API Routes           │
│    └─ user-dashboard.html│         │    ├─ /api/auth          │
│                          │         │    ├─ /api/books         │
│  🎨 CSS Styles           │         │    ├─ /api/users         │
│    ├─ main.css           │         │    ├─ /api/activities    │
│    ├─ dashboard.css      │         │    ├─ /api/submissions   │
│    └─ responsive.css     │         │    └─ /api/settings      │
│                          │         │                          │
│  ⚡ JavaScript           │         │  🗄️  Database Layer      │
│    ├─ config.js     ◄────┼─────────┼──► ├─ db.js             │
│    ├─ app.js             │  HTTP   │    ├─ submissions_db.js  │
│    ├─ dashboard.js       │ Requests│    ├─ library.db         │
│    └─ user-dashboard.js  │         │    └─ submissions.db     │
│                          │         │                          │
│  🌐 HTTP Server          │         │  🔐 Middleware           │
│    └─ http-server        │         │    ├─ CORS               │
│                          │         │    ├─ express-session    │
└──────────────────────────┘         │    ├─ express.json       │
                                     │    └─ authentication     │
                                     │                          │
                                     └──────────────────────────┘
```

## Request Flow

```
1. USER INTERACTION
   │
   ├─► Browser loads http://localhost:8080
   │   └─► Frontend HTML/CSS/JS served
   │
2. API REQUEST
   │
   ├─► User clicks button (e.g., "Login")
   │   └─► JavaScript: fetch('/api/auth/login', {...})
   │       └─► config.js intercepts
   │           └─► Converts to: http://localhost:3000/api/auth/login
   │
3. BACKEND PROCESSING
   │
   ├─► Express receives request at port 3000
   │   ├─► CORS middleware checks origin
   │   ├─► Session middleware loads session
   │   ├─► Body parser parses JSON
   │   └─► Routes to auth.js handler
   │       ├─► Validates credentials
   │       ├─► Queries database
   │       └─► Returns JSON response
   │
4. FRONTEND UPDATE
   │
   └─► JavaScript receives response
       └─► Updates UI accordingly
```

## Data Flow Diagram

```
┌──────────┐
│  BROWSER │
└────┬─────┘
     │
     │ (1) HTTP Request
     ▼
┌─────────────────┐
│  FRONTEND       │
│  Static Files   │
└────┬────────────┘
     │
     │ (2) fetch('/api/...')
     ▼
┌─────────────────┐
│  config.js      │
│  URL Resolver   │
└────┬────────────┘
     │
     │ (3) http://localhost:3000/api/...
     ▼
┌─────────────────┐
│  BACKEND        │
│  Express Server │
└────┬────────────┘
     │
     │ (4) Route Handler
     ▼
┌─────────────────┐
│  DATABASE       │
│  SQLite         │
└────┬────────────┘
     │
     │ (5) Query Result
     ▼
┌─────────────────┐
│  RESPONSE       │
│  JSON Data      │
└────┬────────────┘
     │
     │ (6) Update UI
     ▼
┌──────────┐
│  BROWSER │
└──────────┘
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                         FRONTEND                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │              │    │              │    │              │  │
│  │  index.html  │    │ dashboard    │    │    user-     │  │
│  │              │    │   .html      │    │  dashboard   │  │
│  │  (Landing)   │    │              │    │              │  │
│  │              │    │   (Admin)    │    │   (User)     │  │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘  │
│         │                   │                    │          │
│         └───────────────────┼────────────────────┘          │
│                             │                               │
│                    ┌────────▼────────┐                      │
│                    │   config.js     │                      │
│                    │  (API Config)   │                      │
│                    └────────┬────────┘                      │
│                             │                               │
└─────────────────────────────┼───────────────────────────────┘
                              │
                      ┌───────▼────────┐
                      │      CORS      │
                      │   Validation   │
                      └───────┬────────┘
                              │
┌─────────────────────────────┼───────────────────────────────┐
│                         BACKEND                              │
├─────────────────────────────┼───────────────────────────────┤
│                             │                               │
│                    ┌────────▼────────┐                      │
│                    │   server.js     │                      │
│                    │  (Express App)  │                      │
│                    └────────┬────────┘                      │
│                             │                               │
│         ┌───────────────────┼───────────────────┐           │
│         │                   │                   │           │
│  ┌──────▼──────┐    ┌───────▼──────┐    ┌──────▼──────┐   │
│  │   auth.js   │    │   books.js   │    │  users.js   │   │
│  │   (Route)   │    │   (Route)    │    │  (Route)    │   │
│  └──────┬──────┘    └───────┬──────┘    └──────┬──────┘   │
│         │                   │                   │           │
│  ┌──────▼──────┐    ┌───────▼──────┐    ┌──────▼──────┐   │
│  │activities.js│    │submissions.js│    │ settings.js │   │
│  │   (Route)   │    │   (Route)    │    │  (Route)    │   │
│  └──────┬──────┘    └───────┬──────┘    └──────┬──────┘   │
│         │                   │                   │           │
│         └───────────────────┼───────────────────┘           │
│                             │                               │
│                    ┌────────▼────────┐                      │
│                    │   db.js         │                      │
│                    │  (DB Manager)   │                      │
│                    └────────┬────────┘                      │
│                             │                               │
│                    ┌────────▼────────┐                      │
│                    │  SQLite DB      │                      │
│                    │  - library.db   │                      │
│                    │  - submissions  │                      │
│                    └─────────────────┘                      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## Development vs Production

### Development Setup

```
┌─────────────────┐         ┌─────────────────┐
│   FRONTEND      │         │    BACKEND      │
│                 │         │                 │
│ localhost:8080  │ ◄─────► │ localhost:3000  │
│                 │  CORS   │                 │
│  http-server    │         │   nodemon       │
└─────────────────┘         └─────────────────┘
```

### Production Setup (Option 1: Same Server)

```
┌───────────────────────────────────────┐
│         NGINX (Port 80/443)           │
├───────────────────────────────────────┤
│                                       │
│  ┌─────────────────────────────────┐ │
│  │ Static Files (/frontend/)       │ │
│  │ HTML, CSS, JS                   │ │
│  └─────────────────────────────────┘ │
│                                       │
│  ┌─────────────────────────────────┐ │
│  │ Proxy /api/* → localhost:3000   │ │
│  └─────────────────────────────────┘ │
│                                       │
└───────────────┬───────────────────────┘
                │
                ▼
        ┌───────────────┐
        │  Node.js      │
        │  Backend      │
        │  (Port 3000)  │
        └───────────────┘
```

### Production Setup (Option 2: Separate Hosting)

```
┌─────────────────┐         ┌─────────────────┐
│   Netlify/      │         │   Heroku/       │
│   Vercel        │         │   Railway       │
│                 │         │                 │
│   FRONTEND      │ ◄─────► │    BACKEND      │
│                 │  HTTPS  │                 │
│   Static CDN    │  CORS   │   Node.js       │
└─────────────────┘         └─────────────────┘
```

## File Structure Comparison

### Before (Monolithic)

```
library-system/
├── server.js              ← Mixed concerns
├── routes/                ← Backend
├── database/              ← Backend
├── public/                ← Frontend
│   ├── scripts/
│   ├── styles/
│   └── *.html
└── package.json           ← One package file
```

### After (Separated)

```
library-system/
├── backend/               ← Clean separation
│   ├── server.js
│   ├── routes/
│   ├── database/
│   └── package.json
├── frontend/              ← Independent frontend
│   ├── scripts/
│   ├── styles/
│   ├── *.html
│   └── package.json
└── package.json           ← Root orchestrator
```

## API Endpoint Structure

```
Backend Server (localhost:3000)
│
├─ /api/
│   ├─ /auth/
│   │   ├─ POST /login
│   │   ├─ POST /register
│   │   ├─ POST /logout
│   │   └─ GET  /me
│   │
│   ├─ /books/
│   │   ├─ GET    /
│   │   ├─ POST   /
│   │   ├─ PUT    /:id
│   │   ├─ DELETE /:id
│   │   ├─ GET    /borrowed
│   │   └─ POST   /import
│   │
│   ├─ /users/
│   │   ├─ GET    /
│   │   ├─ POST   /
│   │   ├─ PUT    /:id
│   │   └─ DELETE /:id
│   │
│   ├─ /activities/
│   │   ├─ GET  /
│   │   └─ POST /
│   │
│   ├─ /submissions/
│   │   ├─ GET  /
│   │   ├─ POST /
│   │   ├─ GET  /notifications
│   │   └─ GET  /rating
│   │
│   └─ /settings/
│       ├─ GET /
│       └─ PUT /
│
├─ /api/health
└─ /api/welcome
```

---

This architecture provides:
✅ Clear separation of concerns
✅ Independent scalability
✅ Flexible deployment options
✅ Better development workflow
✅ Easier testing and maintenance
